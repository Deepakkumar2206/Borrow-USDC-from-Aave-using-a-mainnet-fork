[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"

# Let tests select a fork named "mainnet" via vm.createSelectFork("mainnet")
[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"
